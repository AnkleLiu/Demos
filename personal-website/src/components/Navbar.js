import React from 'react'

const Navbar = () => {
    return (
        <div>
            <button type="button" className="btn btn-primary">Danger!</button>
        </div>
    )
}

export default Navbar